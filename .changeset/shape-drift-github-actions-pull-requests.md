---
'@rawdash/connector-github': patch
---

Widen the `pull_requests` schema (\_links, assignees, auto_merge, base, head, labels, requested_reviewers, requested_teams) to accept observed upstream shapes (RAW-727).
