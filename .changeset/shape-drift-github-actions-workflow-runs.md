---
'@rawdash/connector-github': patch
---

Widen the `workflow_runs` schema (head_commit, head_repository, repository, pull_requests, referenced_workflows) to accept observed upstream shapes (RAW-732).
