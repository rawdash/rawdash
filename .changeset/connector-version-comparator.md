---
'@rawdash/core': patch
---

Export `compareConnectorVersions(a, b)` and `latestVersion(versions)`, small utilities for dotted-numeric ordering and max-version selection over connector version strings.
