{
  "name": "website",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -w run docs:connectors && astro dev",
    "build": "pnpm -w run docs:connectors && astro build",
    "preview": "astro preview",
    "typecheck": "pnpm -w run docs:connectors && astro check"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.2.0",
    "@astrojs/starlight": "^0.33.0",
    "astro": "^5.0.0",
    "marked": "^15.0.0",
    "sharp": "^0.33.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "@rawdash/connectors": "workspace:*",
    "typescript": "^5.7.2"
  }
}
