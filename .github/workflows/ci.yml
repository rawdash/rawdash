name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  changeset:
    name: Changeset check
    if: github.event_name == 'pull_request' && github.head_ref != 'changeset-release/main'
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Detect publishable code changes
        id: filter
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
        run: |
          CHANGED=$(git diff --name-only "$BASE_SHA" HEAD \
            | grep -E '^packages/.+\.(ts|tsx|js|mjs|cjs|jsx|json)$' \
            | grep -Ev '^packages/connector-shared/' \
            | grep -Ev '\.(test|spec)\.[^/]+$' \
            | grep -Ev '(^|/)(__tests__|tests)/' \
            || true)
          if [ -n "$CHANGED" ]; then
            echo "has_publishable=true" >> "$GITHUB_OUTPUT"
            echo "Publishable source changes detected:"
            echo "$CHANGED"
          else
            echo "has_publishable=false" >> "$GITHUB_OUTPUT"
            echo "No publishable source changes; changeset not required."
          fi

      - name: Setup pnpm
        if: steps.filter.outputs.has_publishable == 'true'
        uses: pnpm/action-setup@v4
        with:
          version: 10.33.0

      - name: Setup Node.js
        if: steps.filter.outputs.has_publishable == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        if: steps.filter.outputs.has_publishable == 'true'
        run: pnpm install --frozen-lockfile

      - name: Verify changeset for publishable package changes
        if: steps.filter.outputs.has_publishable == 'true'
        run: pnpm changeset status --since=origin/${{ github.event.pull_request.base.ref }}

  ci:
    name: CI
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.33.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Format check
        run: pnpm format:check

      - name: Check connector publishing prerequisites
        env:
          GITHUB_BASE_REF: ${{ github.event.pull_request.base.ref }}
        run: npx tsx scripts/check-connector-publishing-prereqs.ts

      - name: Determine affected filter
        id: affected
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            BASE="${{ github.event.pull_request.base.sha }}"
          elif [ "${{ github.event.before }}" = "0000000000000000000000000000000000000000" ]; then
            BASE="$(git rev-parse HEAD^1)"
          else
            BASE="${{ github.event.before }}"
          fi
          echo "filter=...[${BASE}...HEAD]" >> $GITHUB_OUTPUT

      - name: Verify libsql migrations bundle is up-to-date
        run: |
          pnpm --filter @rawdash/adapter-libsql db:bundle
          if ! git diff --exit-code -- packages/adapters/libsql/src/migrations-bundle.ts; then
            echo "::error::packages/adapters/libsql/src/migrations-bundle.ts is stale. Run \`pnpm --filter @rawdash/adapter-libsql db:bundle\` after adding/modifying SQL migrations and commit the result."
            exit 1
          fi

      - name: Verify connector docs are up-to-date
        run: pnpm docs:connectors:check

      - name: Lint
        run: pnpm turbo run lint --filter="${{ steps.affected.outputs.filter }}"

      - name: Typecheck
        run: pnpm turbo run typecheck --filter="${{ steps.affected.outputs.filter }}"

      - name: Build
        run: pnpm turbo run build --filter="${{ steps.affected.outputs.filter }}"

      - name: Test
        run: pnpm turbo run test --filter="${{ steps.affected.outputs.filter }}"

      - name: Get Playwright version
        id: playwright-version
        run: echo "version=$(pnpm --filter example-nextjs exec playwright --version | awk '{print $2}')" >> "$GITHUB_OUTPUT"

      - name: Cache Playwright browsers
        id: playwright-cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ steps.playwright-version.outputs.version }}

      - name: Install Playwright browsers
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        run: pnpm --filter example-nextjs exec playwright install --with-deps chromium

      - name: Install Playwright system deps
        if: steps.playwright-cache.outputs.cache-hit == 'true'
        run: pnpm --filter example-nextjs exec playwright install-deps chromium

      - name: E2E tests
        run: pnpm turbo run test:e2e --filter="${{ steps.affected.outputs.filter }}"
